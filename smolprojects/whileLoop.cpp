#include <cmath>
#include <iostream>

using namespace std;
int main() {
    unsigned int i = 2;
    while (true) {
        cout << i << endl;
        i = pow(i, 2);
        cout << i << endl;
        // system("pause");
    }

    return 0;  // End program
}
