#include <iostream>
using namespace std;
int main() {
    for (int i = 1; i <= 10; i++) {  // Switched i > 0  to i <= 10
        cout << "i = " << i << endl;
    }
    // system("pause"); // Don't need because on linux
    return 0;
}
